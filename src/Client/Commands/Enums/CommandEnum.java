package Client.Commands.Enums;

public enum CommandEnum
{
    Empty,
    Stop,
    GiveAwayCards,
    PlaceOnTable,
    SendPlayerMoveToServer,
    UpdateInfo,
    Authorize,
    RegisterPokerPlayer,
    PlayerMove
}
